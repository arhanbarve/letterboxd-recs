# Project instructions

## Git
- Always work directly on `main`. Never create branches. Never suggest branching.
- Commit to `main` when work is complete (this overrides any default to branch first on the default branch).
